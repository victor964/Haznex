{
  "build": {"builder": "NIXPACKS"},
  "deploy": {
    "startCommand": "cd backend && gunicorn vhbridge.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "restartPolicyType": "ON_FAILURE"
  }
}
