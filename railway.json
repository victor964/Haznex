{
  "build": {"builder": "NIXPACKS"},
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --run-syncdb && python manage.py collectstatic --noinput && gunicorn vhbridge.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "restartPolicyType": "ON_FAILURE"
  }
}
